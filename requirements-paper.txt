# Optional dependencies for the real-data paper workloads.
torchvision>=0.24
datasets>=4.0
transformers>=4.55
ogb>=1.3.6
torch-geometric>=2.6

# HydraGNN and fairchem-core should be installed by the machine-specific HPC
# scripts because their PyTorch accelerator wheels are platform dependent.
